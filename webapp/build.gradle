apply plugin: 'war'
apply plugin: 'jetty'

dependencies {
    compile project(':mjw-common')
    compile project(':mjw-domain')
    providedCompile 'javax.servlet:servlet-api:2.5'
    runtime 'javax.servlet:jstl:1.1.2'
}
